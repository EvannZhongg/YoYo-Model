interface:
  display_name: "Leakage-Safe Dataset Rebuild"
  short_description: "Preserve reviewed edits during dataset rebuilds"
  default_prompt: "Use $rebuild-leakage-safe-dataset to add approved data while preserving workbench edits, human review mappings, and existing split lineage."
