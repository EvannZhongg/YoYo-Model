interface:
  display_name: "Leakage-Safe Dataset Rebuild"
  short_description: "Rebuild datasets without split leakage"
  default_prompt: "Use $rebuild-leakage-safe-dataset to plan and rebuild reviewed incremental data without moving existing samples or leaking sources across train, validation, and test."
